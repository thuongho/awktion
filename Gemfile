source 'https://rubygems.org'


gem 'rails', '4.2.1'
gem 'sqlite3'
gem 'sass-rails', '~> 5.0'
gem 'uglifier', '>= 1.3.0'
gem 'coffee-rails', '~> 4.1.0'

gem 'jquery-rails'
gem 'turbolinks'
# json
gem 'jbuilder', '~> 2.0'
gem 'sdoc', '~> 0.4.0', group: :doc

gem 'haml-rails'
gem 'bootstrap-sass'
gem 'autoprefixer-rails'
# server that supports threading, multiple websockets
gem 'puma'
gem 'devise'
# core of websockets, multiple connection via websockets
gem 'faye-websocket'
gem 'pry'
# table layout in console
gem 'hirb'


group :development, :test do
  gem 'byebug'
  gem 'web-console', '~> 2.0'
  gem 'spring'
end

group :test do
  # clean slate before every single task is run
  gem 'database_cleaner'
  # manipulate time, test if you win auction
  gem 'timecop'
  # extension to pry to test 
  gem 'pry-byebug'
end